# Billboard

Billboard is a full-stack TypeScript kanban-style task board (portfolio project) with real-time collaboration built using React + Vite, Node + Express, PostgreSQL + Prisma, and Socket.IO. It includes Docker and a GitHub Actions CI workflow.

See instructions in the repository for running locally with docker-compose and seeding the database.